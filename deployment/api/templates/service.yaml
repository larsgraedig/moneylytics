apiVersion: v1
kind: Service
metadata:
  name: {{ include "moneylytics.fullname" . }}
  labels:
    {{- include "moneylytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moneylytics.selectorLabels" . | nindent 4 }}
