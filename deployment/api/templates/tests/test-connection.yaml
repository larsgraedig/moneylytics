apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moneylytics.fullname" . }}-test-connection"
  labels:
    {{- include "moneylytics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moneylytics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
